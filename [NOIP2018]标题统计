#include <stdio.h>
#include <string.h>
int main()
{
	char ch[100];
	gets(ch);
	int i = 0;
	int count = 0;
	int str = strlen(ch);
	for (i = 0; i <str; i++)
	{
		if (ch[i] != '\n' && ch[i] != ' ')
			count++;
	}
	printf("%d", count);
	return 0;
}